// Utilise par defaut (ng serve). Remplace par environment.prod.ts lors d'un build de production
// (voir "fileReplacements" dans angular.json).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};
